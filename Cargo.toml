[package]
name = "blake3_wasm_streaming"
version = "0.2.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
blake3 = { path = "../BLAKE3", features = ["wasm32_simd"] }

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
panic = "abort"
