GPT 5.4 says this is the build command

cargo +nightly build -Z build-std=std,panic_abort --release
wasm-bindgen --target web --out-dir pkg target/wasm32-unknown-unknown/release/blake3_wasm_streaming.wasm
